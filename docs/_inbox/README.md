# Orca Inbox

This folder is temporary scratch for untriaged prompts, notes, imports, and parked material.

Nothing in `_inbox` is Orca authority. Promote material into an accepted docs folder or add it to `docs/hygiene/queue.md` before citing it as source.
