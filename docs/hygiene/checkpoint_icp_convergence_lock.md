# Convergence Lock Checkpoint (ICP redo)

Non-authoritative checkpoint (Orca overlay `source-of-truth.md` checkpoint
lifecycle): single-consumption, one live instance per lane, point-don't-copy.
Never overrides canonical source; re-confirm volatile claims before relying.

## Pointers (re-confirm on use)

- Authoritative lock + propagation blocker + cascade proposal ->
  `docs/decisions/orca_icp_wedge_convergence_break_in_first_v0.md` (reread first).
- Campaign evidence ledger ->
  `docs/research/orca_icp_redo_evidence_targets_v0.md`.
- Evidence, verified primary (owner's external DR) ->
  `C:\Users\vmon7\Documents\deep-research-report for opus.md`.
- Evidence, unverified cross-check (my parallel run; harness VERIFICATION FAILED,
  leads only, do not treat as confirmed) -> task output `w7zz1zzma`.

## Locked state (pointer, not copy)

Break-in-first / pricing-expansion, on one outside-in competitive/market
intelligence engine; situational buyer (outside-in-dependent moment, not a
segment). Owner-locked 2026-06-08. Canonical detail -> the decision record above.

## Exact next action

1. POKE: stress-test the locked direction (owner: "we'll poke a little bit").
2. Then: owner-authorized product-lane cascade per the decision record's blocker
   — ICP wedge v1 -> offer hypothesis v1 -> buyer-proof packet + proof-lead
   charter patches -> candidate skill refresh; each with its own DCP receipt.

## Drift guards / non-claims

- All lane docs are untracked on the ECR branch (`ecr-sp3-timing-deriver-slice1`);
  durable home is main; committing is the owner's call via the product lane.
- Break-in evidence is partly vendor-published -> discount adoption/ROI. Nothing
  here is validation, WTP, buyer proof, readiness, or ICP-proven.
- Do NOT clobber `.agents/workflow-overlay/skill-adoption.md` (another lane
  modifies it).

## Lifecycle

Single-consumption; one live instance per lane. Burn (delete) once the poke
session re-establishes state or a fresh checkpoint replaces it. Do not leave a
spent checkpoint to be re-trusted as current.
